spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:certs/jwt-public.pem

server:
  ssl:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health